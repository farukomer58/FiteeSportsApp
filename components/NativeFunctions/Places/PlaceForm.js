import React from 'react'
import { View, Text, Image, Pressable, StyleSheet } from "react-native"

export default PlaceForm = props => {
    return <View>
        <Text>The Place Form</Text>
    </View>
}

const styles = StyleSheet.create({

});